# renga

**AI ネイティブなエージェント用ターミナル** — 複数の Claude Code / Codex / GitHub Copilot CLI エージェントを TUI 分割ペインで協調させる Rust 製ツール。

![中央に開いた IME composition overlay で日本語を入力している様子。候補窓はキャレット直下に吸着し、背後の Claude pane は凍結されている](/ime-overlay.png)

![renga スクリーンショット](/screenshot.png)

## 概要

renga は、複数のコーディングエージェントを並列で動かし協調させることに特化した AI ネイティブなターミナルです。PTY の上に vt100 エミュレータを挟んだ薄いレイヤーで、分割ペイン・タブ・ファイルツリーを提供しつつ、ペインを単なる shell ではなくエージェント endpoint として扱います。同じタブの [Claude Code](https://docs.anthropic.com/en/docs/claude-code) / Codex / GitHub Copilot CLI が組み込みの `renga-peers` ネットワークに参加し、ワーカー起動、別ペイン確認、メッセージング、協調までその場で進められるのが差分です。汎用 tmux 代替というより、マルチエージェント開発のローカル司令塔と考えるのが近いです。

## 特徴

- 複数の Claude Code / Codex / GitHub Copilot CLI セッションを分割ペインで並列実行
- **peer メッセージング + ペインオーケストレーション** — 組み込みの `renga-peers` MCP サーバ経由で、同じタブの Claude Code / Codex / Copilot が `list_peers` / `send_message` / `spawn_claude_pane` / `spawn_codex_pane` / `spawn_copilot_pane` / `inspect_pane` / `send_keys` / `poll_events` などを使える
- **中央多行の IME composition overlay** — `Alt+Enter` で送信、`Enter` で改行、候補窓がキャレットに吸着 (renga 独自)
- **ペイン凍結 + 周期 catch-up** (`--ime-freeze-panes` + `--ime-overlay-catchup-ms`) で IME 入力中のちらつきを抑制 (renga 独自)
- ファイルツリーとシンタックスハイライト付きプレビュー
- タブごとに独立したワークスペース
- 単一バイナリ (プラットフォームにより約 8-10 MB)、追加ランタイム不要
- Windows / macOS / Linux 対応

## クイックスタート

```bash
npm install -g @suisya-systems/renga
renga mcp install
renga
```

起動後、ペインで `Alt+P` を押すと peer 対応済みの Claude 起動コマンドが入力されます。Codex / Copilot は [メッセージングの説明](/ja/peer-messaging) にある登録経路から起動できます。同じタブで sibling agent を立ち上げると、各ペインは同じ `renga-peers` ネットワークに参加して、そのまま協調できます。

初回セットアップは [はじめ方](/ja/getting-started)、MCP ツール面は [Claude Code ペイン同士のメッセージング](/ja/peer-messaging) を参照してください。

## 主な機能

- [**マルチペイン分割**](/ja/features#ペイン管理) — 縦横分割、最大 16 ペイン
- [**peer メッセージング + ペイン制御**](/ja/peer-messaging) — Claude Code / Codex / Copilot 間の会話に加えて、分割、ワーカー起動、画面確認、対話プロンプト応答、寿命監視までできる
- [**IME composition overlay**](/ja/features#ime-入力-日本語--中国語--韓国語) — 中央多行、`Alt+Enter` 送信、候補窓吸着、凍結 + 周期 catch-up
- [**ファイルツリー + プレビュー**](/ja/features#ファイルツリー) — シンタックスハイライト付きファイル閲覧
- [**タブワークスペース**](/ja/features#タブ) — タブごとに独立したワークスペース
- [**エージェント検出**](/ja/features#エージェント検出) — client ごとにペイン枠を色分け（Claude Code はオレンジ、Codex は緑、Copilot は紫）
- [**テキスト選択**](/ja/features#テキスト選択とコピー) — ドラッグで選択、自動クリップボードコピー
- [**スクロールバック**](/ja/features#ターミナルスクロールバック) — ペインごと 10,000 行の履歴
